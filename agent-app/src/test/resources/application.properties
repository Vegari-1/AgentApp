# CONFIG
stage=${STAGE:dev}
cors.origin=${CORS_ORIGIN:http://localhost:3000}

# TestContainers
spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver
spring.datasource.url=jdbc:tc:postgresql:11:///AgentApp_DB
